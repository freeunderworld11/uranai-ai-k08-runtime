{
  "name": "uranai-ai-k08-runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Workers runtime shell for K08 Swiss Ephemeris integration",
  "license": "AGPL-3.0-only",
  "engines": { "node": ">=22" },
  "scripts": {
    "dev": "wrangler dev",
    "test": "node --test",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy"
  },
  "devDependencies": { "wrangler": "4.130.0" }
}
